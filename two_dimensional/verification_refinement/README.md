# Independent temporal-discretisation check

`summary.json` records L1 residuals for Benchmark A at `n_t = 90, 179, 357` on the fixed `80 x 80` spatial grid. The observed mean rate is compared with the expected L1 rate `2-alpha`. Data generation itself uses semi-analytic Mittag--Leffler propagation.
