# Reported two-dimensional results

This directory contains the independently reproduced 70-run archive used by the manuscript: 25 runs for Benchmark A, 25 for Benchmark B, and 20 spatial-window sensitivity runs. `summary_2d.csv` is derived from the three per-run JSONL files. The reproduction manifest records the environment, data hashes, search settings, and weak-row count.
